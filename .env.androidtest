# Test APK (`vite build --mode androidtest`): the API's isolated test
# environment, a TEST banner, and the test-only logins on the sign-in page.
VITE_API_BASE_URL=https://flexiloans-hrms-api.netlify.app/test
VITE_APP_ENV=test
